% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmap_visualization.R
\name{plot_signature_direction_tile_barcode}
\alias{plot_signature_direction_tile_barcode}
\title{Plot 2D Barcode Heatmap from GCTX (Single Drug Context)}
\usage{
plot_signature_direction_tile_barcode(
  results_df = NULL,
  signature_file = NULL,
  reference_df = NULL,
  gctx_file = NULL,
  geneinfo_file = NULL,
  siginfo_file = NULL,
  data_dir = getOption("CONCERTDR.data_dir", NULL),
  selected_drug = NULL,
  selected_drug_col = NULL,
  pert_id_col = "compound",
  score_col = "Score",
  max_genes = 100,
  max_perts = 60,
  cluster_rows = TRUE,
  cluster_method = "complete",
  show_row_dendrogram = TRUE,
  cluster_cols = FALSE,
  cluster_method_cols = "complete",
  show_col_dendrogram = TRUE,
  precomputed = NULL,
  save_png = FALSE,
  output_png = "barcode_heatmap.png",
  output_zscores = NULL,
  width = NULL,
  height = NULL,
  dpi = 150,
  verbose = TRUE
)
}
\arguments{
\item{results_df}{Data frame (typically \code{tech_view_all}) containing at
least perturbation id and score columns.}

\\item{signature_file}{Path to a signature file with gene and log2FC columns,\n  or a data frame with \\code{Gene} and \\code{log2FC} columns.}

\item{reference_df}{Optional reference matrix with genes as rows and
perturbation ids as columns. If supplied, no GCTX file is needed and the
heatmap is built directly from this matrix.}

\item{gctx_file}{Optional path to GCTX expression file. If NULL, the function
tries (in order): option \code{CONCERTDR.gctx_file}, env var
\code{CONCERTDR_GCTX_FILE}, \code{data_dir/level5_beta_all_n1201944x12328.gctx},
then a file with that name in the working directory.}

\item{geneinfo_file}{Optional path to geneinfo file with \code{gene_symbol}
and \code{gene_id} columns. If NULL, the function tries option
\code{CONCERTDR.geneinfo_file}, env var \code{CONCERTDR_GENEINFO_FILE},
\code{data_dir/geneinfo_beta.txt}, then \code{geneinfo_beta.txt} in the
working directory.}

\item{siginfo_file}{Optional path to siginfo file for readable perturbation
labels. If NULL, the function tries option \code{CONCERTDR.siginfo_file},
env var \code{CONCERTDR_SIGINFO_FILE}, \code{data_dir/siginfo_beta.txt},
then \code{siginfo_beta.txt} in the working directory.}

\item{data_dir}{Optional directory containing CMap files. Useful to avoid
repeatedly passing full file paths.}

\item{selected_drug}{Optional drug identifier to filter \code{results_df}
before selecting perturbations.}

\item{selected_drug_col}{Optional column used with \code{selected_drug}.
If NULL, auto-detect from \code{display_name}, \code{pert_id},
\code{cmap_name}, \code{pert_name}.}

\item{pert_id_col}{Perturbation id column in \code{results_df}
(default: \code{"compound"}, i.e. sig_id in CONCERTDR results).}

\item{score_col}{Score column in \code{results_df} (default: \code{"Score"}).}

\item{max_genes}{Maximum number of signature genes to use (default: 100).}

\item{max_perts}{Maximum number of perturbations to show (default: 60).}

\item{cluster_rows}{Logical; cluster perturbation rows (default: TRUE).}

\item{cluster_method}{Clustering linkage method for row clustering
(default: \code{"complete"}).}

\item{show_row_dendrogram}{Logical; whether to draw a row dendrogram when
\code{cluster_rows = TRUE} (default: TRUE).}

\item{cluster_cols}{Logical; cluster gene columns (default: FALSE — genes
are kept in signature direction order: down then up).}

\item{cluster_method_cols}{Clustering linkage method for column clustering
(default: \code{"complete"}).}

\item{show_col_dendrogram}{Logical; whether to draw a column dendrogram
when \code{cluster_cols = TRUE} (default: TRUE).}

\item{precomputed}{Optional; the output of \code{\link{extract_signature_zscores}}.
If provided, skips all data-loading and GCTX-extraction steps — all
data-related arguments (\code{results_df}, \code{signature_file},
\code{gctx_file}, etc.) are ignored.}

\item{save_png}{Logical; save PNG output (default: FALSE).}

\item{output_png}{Output PNG path.}

\item{output_zscores}{Optional TSV path for exported z-score matrix.
Default is \code{NULL}, so no file is written unless explicitly requested.}

\item{width, height}{Figure dimensions in inches for PNG. If \code{NULL}
(default), dimensions are computed automatically from the number of genes
and perturbations (~0.22 in/gene width, ~0.28 in/perturbation height).}

\item{dpi}{PNG resolution.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly returns a list containing the plotted matrix,
selected perturbation ids, and file paths.
}
\description{
Build a 2D barcode heatmap using z-scores extracted from a GCTX file.
Genes are ordered by signature direction (down then up), and perturbations
are chosen as top \code{max_perts} rows by best (lowest) \code{score_col}
from the provided technical results table.
}
\examples{
is.function(plot_signature_direction_tile_barcode)

\donttest{
if (requireNamespace("ComplexHeatmap", quietly = TRUE) &&
    requireNamespace("circlize", quietly = TRUE)) {
  sig_file <- system.file("extdata", "example_signature.txt",
                          package = "CONCERTDR")
  plot_signature_direction_tile_barcode(
    results_df    = data.frame(sig_id = "DEMO001", Score = -0.72),
    signature_file = sig_file,
    reference_df  = read.csv(system.file("extdata", "example_reference_df.csv",
                                         package = "CONCERTDR"),
                             row.names = 1, check.names = FALSE),
    pert_id_col   = "sig_id"
  )
}
}

}
