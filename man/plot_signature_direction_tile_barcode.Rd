% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmap_visualization.R
\name{plot_signature_direction_tile_barcode}
\alias{plot_signature_direction_tile_barcode}
\title{Plot Signature Direction Tile Barcode}
\usage{
plot_signature_direction_tile_barcode(
  results_df,
  signature_file,
  display_col = "display_name",
  score_col = "Score",
  max_pert = 200,
  max_genes = NULL,
  palette = c("#3B4CC0", "#F7F7F7", "#B40426"),
  save_png = FALSE,
  output_dir = NULL,
  output_file = NULL,
  width = 20,
  height = 12,
  dpi = 300,
  verbose = TRUE
)
}
\arguments{
\item{results_df}{Data frame containing at least a display-name column and a score column.}

\item{signature_file}{Path to a signature file (CSV/TSV) with gene and log2FC columns.}

\item{display_col}{Gene label column in \code{results_df} (default: \code{"display_name"}).}

\item{score_col}{Score column in \code{results_df} (default: \code{"Score"}).}

\item{max_pert}{Maximum number of perturbation tiles per gene (default: \code{200}).
Use \code{NULL} to keep all.}

\item{max_genes}{Optional cap on number of genes per panel (default: \code{NULL}).}

\item{palette}{Color palette function name accepted by
\code{grDevices::colorRampPalette} (default: \code{c("#3B4CC0", "#F7F7F7", "#B40426")}).}

\item{save_png}{Logical; whether to write PNG output (default: \code{FALSE}).}

\item{output_dir}{Output directory when \code{save_png = TRUE} and \code{output_file} is NULL.}

\item{output_file}{Optional full output file path. If provided and
\code{save_png = TRUE}, this path is used directly.}

\item{width, height}{Plot size in inches for PNG output.}

\item{dpi}{PNG resolution.}

\item{verbose}{Logical; print progress messages.}
}
\value{
Invisibly returns a list with \code{ranked_data}, \code{matched_genes}, and
\code{output_file} (if written).
}
\description{
Create a 2-panel barcode-style plot (UP and DOWN signature directions)
where each gene is an x-axis column and each column is split into tiles for
perturbation scores. Tile color is mapped to score with a symmetric palette
centered at 0.
}
\examples{
\dontrun{
views <- annotate_drug_results(
  results_df = results$results$ks,
  sig_info_file = filtered_siginfo,
  comp_info_file = "compoundinfo_beta.txt"
)

plot_signature_direction_tile_barcode(
  results_df = views$wetlab_gene_view,
  signature_file = "signature.txt",
  save_png = TRUE,
  output_dir = "results"
)
}

}
