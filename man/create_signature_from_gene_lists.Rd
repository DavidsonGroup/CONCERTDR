% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature_matching.R
\name{create_signature_from_gene_lists}
\alias{create_signature_from_gene_lists}
\title{Create a Signature Data Frame from Gene Lists}
\usage{
create_signature_from_gene_lists(
  up_genes,
  down_genes,
  up_value = 1,
  down_value = -1
)
}
\arguments{
\item{up_genes}{Character vector of up-regulated gene symbols.}

\item{down_genes}{Character vector of down-regulated gene symbols.}

\item{up_value}{Numeric value to assign to up-regulated genes (default: 1).}

\item{down_value}{Numeric value to assign to down-regulated genes (default: -1).}
}
\value{
A data frame with columns \code{Gene} and \code{log2FC}, suitable for
  use as the \code{signature_file} argument in
  \code{\link{process_signature_with_df}} and
  \code{\link{extract_signature_zscores}}.
}
\description{
Converts separate lists of up-regulated and down-regulated genes into a
signature data frame compatible with \code{\link{process_signature_with_df}}
and other CONCERTDR functions.  This is a convenience wrapper for users who
only have gene lists (e.g.\ from a pathway database) rather than continuous
fold-change values.
}
\examples{
sig_df <- create_signature_from_gene_lists(
  up_genes   = c("TP53", "MYC", "BRCA1"),
  down_genes = c("EGFR", "VEGFA", "KRAS")
)
head(sig_df)

}
